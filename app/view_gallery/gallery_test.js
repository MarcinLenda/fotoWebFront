'use strict';

describe('myApp.view_gallery module', function() {

  beforeEach(module('myApp.view_gallery'));

  describe('view_gallery controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});