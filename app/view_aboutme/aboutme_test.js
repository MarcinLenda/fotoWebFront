'use strict';

describe('myApp.view_aboutme module', function() {

  beforeEach(module('myApp.view_aboutme'));

  describe('view_aboutme controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('view_aboutmeCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});