'use strict';

describe('myApp.view_login module', function() {

    beforeEach(module('myApp.view_login'));

    describe('view_login controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var loginCtrl = $controller('loginCtrl');
            expect(loginCtrl).toBeDefined();
        }));

    });
});/**
 * Created by promar7 on 10.08.16.
 */
